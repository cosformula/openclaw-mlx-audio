{
  "name": "@cosformula/openclaw-mlx-audio",
  "version": "0.1.10",
  "description": "OpenClaw local TTS plugin powered by mlx-audio — zero API key, zero cloud dependency",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "check-schema": "node scripts/check-schema-sync.mjs",
    "build": "tsc && npm run check-schema",
    "test": "npm run build && node --test dist/test/*.test.js",
    "prepublishOnly": "npm run build"
  },
  "files": [
    "dist/**",
    "python-runtime/**",
    "openclaw.plugin.json",
    "skills/**",
    "README.md",
    "LICENSE"
  ],
  "openclaw": {
    "extensions": [
      "./dist/index.js"
    ]
  },
  "author": "CCC Games <dev@contextcross.com>",
  "license": "MIT",
  "keywords": [
    "openclaw",
    "tts",
    "mlx",
    "apple-silicon",
    "local"
  ],
  "repository": {
    "type": "git",
    "url": "https://github.com/cosformula/openclaw-mlx-audio.git"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "typescript": "^5.7.3"
  }
}
